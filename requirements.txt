# Lakehouse local - NYC Taxi
# Testado com Python 3.12 (dbt-core/dbt-duckdb ainda não têm wheels
# estáveis para versões mais novas do Python em todas as plataformas)

# Engine analítica
duckdb==1.1.3

# Transformação e modelagem
dbt-core==1.8.9
dbt-duckdb==1.8.4

# Processamento de dados
pandas==2.2.3
pyarrow==18.1.0

# HTTP para download do dataset
requests==2.32.3
